name: ordyn-api

services:
  fpm:
    image: docker.hank-it.com/ordyn/fpm:${API_TAG:-stable}
    restart: always
    env_file: ".env"
    environment:
      ORDYN_SERVICE_ROLE: fpm
    volumes:
      - app-storage:/var/www/html/storage
    depends_on:
      database:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - default
      - package-analysis
    labels:
      - "cosign.verify=keys/ordyn-2026-06.pub"
      - "traefik.enable=false"
    extra_hosts:
      - "host.docker.internal:host-gateway"

  web:
    image: docker.hank-it.com/ordyn/web:${API_TAG:-stable}
    restart: always
    volumes:
      - app-storage:/var/www/html/storage
    environment:
      FPM_SERVICE: fpm
    networks:
      - default
      - frontend
    depends_on:
      fpm:
        condition: service_started
      database:
        condition: service_healthy
      redis:
        condition: service_healthy
    labels:
      - "cosign.verify=keys/ordyn-2026-06.pub"
      - "traefik.enable=true"
      - "traefik.http.routers.ordyn-backend-web.rule=Host(`${ORDYN_API_HOST:?err}`)"
      - "traefik.http.routers.ordyn-backend-web.entrypoints=websecure"
      - "traefik.http.services.ordyn-backend-web.loadbalancer.server.port=8080"

  scheduler:
    image: docker.hank-it.com/ordyn/fpm:${API_TAG:-stable}
    restart: always
    env_file: ".env"
    environment:
      ORDYN_SERVICE_ROLE: scheduler
    entrypoint: ordyn-observed
    command:
      - php artisan schedule:work
    volumes:
      - app-storage:/var/www/html/storage
    labels:
      - "cosign.verify=keys/ordyn-2026-06.pub"
      - "traefik.enable=false"
    depends_on:
      database:
        condition: service_healthy
      redis:
        condition: service_healthy
    extra_hosts:
      - "host.docker.internal:host-gateway"

  horizon:
    image: docker.hank-it.com/ordyn/fpm:${API_TAG:-stable}
    restart: always
    env_file: ".env"
    environment:
      ORDYN_SERVICE_ROLE: horizon
    entrypoint: ordyn-observed
    stop_signal: SIGTERM
    networks:
      - default
      - package-analysis
    command:
      - php artisan horizon
    volumes:
      - app-storage:/var/www/html/storage
    labels:
      - "cosign.verify=keys/ordyn-2026-06.pub"
      - "traefik.enable=false"
    depends_on:
      redis:
        condition: service_healthy
      database:
        condition: service_healthy
    extra_hosts:
      - "host.docker.internal:host-gateway"

  edge-bridge:
    image: docker.hank-it.com/ordyn/fpm:${API_TAG:-stable}
    restart: always
    env_file: ".env"
    environment:
      ORDYN_SERVICE_ROLE: edge-bridge
    entrypoint: ordyn-observed
    networks:
      - default
    command:
      - php artisan ordyn:edge-bridge
    volumes:
      - app-storage:/var/www/html/storage
    labels:
      - "cosign.verify=keys/ordyn-2026-06.pub"
      - "traefik.enable=false"
    depends_on:
      database:
        condition: service_healthy
      redis:
        condition: service_healthy
    extra_hosts:
      - "host.docker.internal:host-gateway"

  cache-bridge:
    image: docker.hank-it.com/ordyn/fpm:${API_TAG:-stable}
    restart: always
    env_file: ".env"
    entrypoint: ordyn-observed
    environment:
      ORDYN_SERVICE_ROLE: cache-bridge
    networks:
      - default
    command:
      - php artisan ordyn:cache-bridge
    volumes:
      - app-storage:/var/www/html/storage
    labels:
      - "cosign.verify=keys/ordyn-2026-06.pub"
      - "traefik.enable=false"
    depends_on:
      database:
        condition: service_healthy
      redis:
        condition: service_healthy
    extra_hosts:
      - "host.docker.internal:host-gateway"

  clamav:
    image: clamav/clamav:${CLAMAV_TAG:-1.5.2}
    restart: unless-stopped
    labels:
      - "traefik.enable=false"
    environment:
      CLAMD_CONF_StreamMaxLength: 15360M
      CLAMD_CONF_MaxScanSize: 15360M
      CLAMD_CONF_MaxFileSize: 15360M
      CLAMD_CONF_ReadTimeout: 1800
      CLAMD_CONF_CommandReadTimeout: 1800
    volumes:
      - "clamav-db:/var/lib/clamav"
    networks:
      - default

  redis:
    image: redis:${REDIS_TAG:-8-alpine}
    restart: unless-stopped
    labels:
      - "traefik.enable=false"
    command:
      - "redis-server"
      - "/usr/local/etc/redis/redis.conf"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    volumes:
      - "redis-storage:/data"
      - "/opt/ordyn-compose/api/redis.conf:/usr/local/etc/redis/redis.conf:ro"

  database:
    image: postgres:${POSTGRES_TAG:-18-alpine}
    restart: unless-stopped
    labels:
      - "traefik.enable=false"
    command:
      - "postgres"
      - "-c"
      - "config_file=/etc/postgresql/postgresql.conf"
    environment:
      POSTGRES_DB: ${DB_DATABASE:?err}
      POSTGRES_USER: ${DB_USERNAME:?err}
      POSTGRES_PASSWORD: ${DB_PASSWORD:?err}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -h 127.0.0.1 -p 5432 -U \"$DB_USERNAME\" -d \"$DB_DATABASE\""]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    volumes:
      - "database-storage:/var/lib/postgresql"
      - "/opt/ordyn-compose/api/postgresql.conf:/etc/postgresql/postgresql.conf:ro"

  websockets:
    image: quay.io/soketi/soketi:${WEBSOCKET_TAG:-1.6-16-alpine}
    restart: unless-stopped
    environment:
      SOKETI_DEBUG: '0'
      SOKETI_DEFAULT_APP_ID: ${WEBSOCKET_APP_ID}
      SOKETI_DEFAULT_APP_KEY: ${WEBSOCKET_APP_KEY}
      SOKETI_DEFAULT_APP_SECRET: ${WEBSOCKET_APP_SECRET}
      SOKETI_EVENT_MAX_BATCH_SIZE: 50
      SOKETI_EVENT_MAX_SIZE_IN_KB: 4096
      SOKETI_HTTP_MAX_REQUEST_SIZE: 128
    networks:
      - frontend
      - default
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.ordyn-soketi.rule=Host(`${ORDYN_API_HOST:?err}`) && PathPrefix(`/ws`)"
      - "traefik.http.routers.ordyn-soketi.entrypoints=websecure"
      - "traefik.http.routers.ordyn-soketi.service=ordyn-soketi"
      - "traefik.http.services.ordyn-soketi.loadbalancer.server.port=6001"
      - "traefik.http.services.ordyn-soketi.loadbalancer.server.scheme=http"
      - "traefik.http.routers.ordyn-soketi.middlewares=ordyn-soketi-stripprefix"
      - "traefik.http.middlewares.ordyn-soketi-stripprefix.stripprefix.prefixes=/ws"

  package-analyzer:
    image: docker.hank-it.com/ordyn/package-analyzer:${API_TAG:-stable}
    restart: unless-stopped
    environment:
      PACKAGE_ANALYZER_MAX_INPUT_BYTES: "${PACKAGE_ANALYZER_MAX_INPUT_BYTES:-16106127360}"
      PACKAGE_ANALYZER_REQUEST_DEADLINE_SECONDS: "${PACKAGE_ANALYZER_REQUEST_DEADLINE_SECONDS:-50}"
      PACKAGE_ANALYZER_MAX_CONCURRENT_ANALYSES: "${PACKAGE_ANALYZER_MAX_CONCURRENT_ANALYSES:-2}"
      PACKAGE_ANALYZER_PROCESS_TIMEOUT_SECONDS: "${PACKAGE_ANALYZER_PROCESS_TIMEOUT_SECONDS:-20}"
      PACKAGE_ANALYZER_MIN_FREE_TEMP_BYTES: "${PACKAGE_ANALYZER_MIN_FREE_TEMP_BYTES:-536870912}"
    volumes:
      - package-analyzer-tmp:/var/tmp/package-analyzer
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    pids_limit: "${PACKAGE_ANALYZER_PIDS_LIMIT:-128}"
    mem_limit: "${PACKAGE_ANALYZER_MEMORY_LIMIT:-2g}"
    cpus: "${PACKAGE_ANALYZER_CPU_LIMIT:-2.0}"
    tmpfs:
      - /tmp:size=64m,mode=1777
    networks:
      - package-analysis
    labels:
      - "cosign.verify=keys/ordyn-2026-06.pub"
      - "traefik.enable=false"

  pdf-renderer:
    image: docker.hank-it.com/ordyn/pdf-renderer:${API_TAG:-stable}
    restart: unless-stopped
    networks:
      - default
    labels:
      - "cosign.verify=keys/ordyn-2026-06.pub"
      - "traefik.enable=false"

volumes:
  database-storage:
  app-storage:
  redis-storage:
  clamav-db:
  package-analyzer-tmp:

networks:
  frontend:
    name: ${FRONTEND_NETWORK_NAME:-frontend}
    external: true
  default:
  package-analysis:
    internal: true
